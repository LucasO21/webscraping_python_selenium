# TIMING AN OPERATION

```start = time.time()```

 ``code to be timed here``

`end = time.time()`
`print("Elapsed time: ", end - start)`

# SCHEDULING A SCRIPT

- Open terminal
- ```crontab -e``` to open crontab
- ```i``` to insert
- ```* * * * /path to python script

- ```Ctrl + X, Y``` to save
- ```esc```
- ```:wq``` to quit crontab
